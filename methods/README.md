# Methods

This dataset repository does not release the implementation code of the compared optimization algorithms.

The directory is intentionally kept as a placeholder to make the repository structure explicit. The released files focus on input data, RF training samples, formal experimental summaries, figures, and lightweight per-run final Pareto fronts.
